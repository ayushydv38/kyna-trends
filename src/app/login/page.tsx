 "use client";
import {useState} from "react";
export default function Login(){const [e,setE]=useState("");const [busy,setB]=useState(false);async function go(x:React.FormEvent<HTMLFormElement>){x.preventDefault();setB(true);const f=new FormData(x.currentTarget);const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:f.get("email"),password:f.get("password")})});if(r.ok)location.href="/";else{const d=await r.json();setE(d.error||"Login failed");setB(false)}}return <main className="mx-auto max-w-md p-8"><h1 className="mb-6 text-3xl font-black">Login</h1>{e&&<p className="mb-3 text-red-600">{e}</p>}<form onSubmit={go} className="space-y-3"><input name="email" type="email" required placeholder="Email" className="w-full rounded-xl border p-3"/><input name="password" type="password" required placeholder="Password" className="w-full rounded-xl border p-3"/><button disabled={busy} className="w-full rounded-xl bg-slate-900 p-3 text-white">{busy?"Signing in...":"Login"}</button></form></main>}