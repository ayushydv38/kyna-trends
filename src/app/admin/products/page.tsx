"use client";
import {useEffect,useState} from "react";
type P={id:string;name:string;slug:string;description:string;price:number;comparePrice:number|null;image:string;stock:number;sku:string;active:boolean;categoryId:string|null};
const empty={name:"",slug:"",description:"",price:0,comparePrice:null as number|null,image:"https://images.unsplash.com/photo-1523275335684-37898b6baf30?w=800",stock:0,sku:"",categoryId:null as string|null,active:true};
export default function Products(){const [ps,setPs]=useState<P[]>([]);const [cats,setCats]=useState<any[]>([]);const [form,setForm]=useState<any>(empty);const [edit,setEdit]=useState<string|null>(null);const [open,setOpen]=useState(false);const load=()=>fetch('/api/admin/products').then(r=>r.json()).then(setPs);useEffect(()=>{load();fetch('/api/admin/categories').then(r=>r.json()).then(setCats)},[]);async function save(){const url=edit?`/api/admin/products/${edit}`:'/api/admin/products';const r=await fetch(url,{method:edit?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,price:Number(form.price),comparePrice:form.comparePrice===''?null:Number(form.comparePrice),stock:Number(form.stock)})});const d=await r.json();if(!r.ok)return alert(d.error||'Save failed');setOpen(false);setEdit(null);setForm(empty);load()}async function disable(id:string){if(!confirm('Deactivate this product?'))return;await fetch(`/api/admin/products/${id}`,{method:'DELETE'});load()}return <div className="p-5 md:p-8"><div className="mb-6 flex items-center justify-between"><div><h1 className="text-3xl font-black">Products</h1><p className="mt-1 text-slate-500">Manage your catalogue and inventory.</p></div><button className="btn btn-primary" onClick={()=>{setForm(empty);setEdit(null);setOpen(true)}}>+ Add product</button></div>{open&&<div className="card mb-6 p-6"><div className="grid gap-4 md:grid-cols-2"><input className="rounded-xl border p-3" placeholder="Product name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input className="rounded-xl border p-3" placeholder="Slug" value={form.slug} onChange={e=>setForm({...form,slug:e.target.value})}/><input className="rounded-xl border p-3" placeholder="SKU" value={form.sku} onChange={e=>setForm({...form,sku:e.target.value})}/><input className="rounded-xl border p-3" placeholder="Image URL" value={form.image} onChange={e=>setForm({...form,image:e.target.value})}/><input type="number" className="rounded-xl border p-3" placeholder="Price" value={form.price} onChange={e=>setForm({...form,price:e.target.value})}/><input type="number" className="rounded-xl border p-3" placeholder="Compare price" value={form.comparePrice??''} onChange={e=>setForm({...form,comparePrice:e.target.value})}/><input type="number" className="rounded-xl border p-3" placeholder="Stock" value={form.stock} onChange={e=>setForm({...form,stock:e.target.value})}/><select className="rounded-xl border p-3" value={form.categoryId??''} onChange={e=>setForm({...form,categoryId:e.target.value||null})}><option value="">No category</option>{cats.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><textarea className="rounded-xl border p-3 md:col-span-2" rows={4} placeholder="Description" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></div><div className="mt-4 flex gap-2"><button className="btn btn-primary" onClick={save}>{edit?'Update':'Create'} product</button><button className="btn btn-light" onClick={()=>setOpen(false)}>Cancel</button></div></div>}<div className="card overflow-hidden"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50"><tr><th className="p-4">Product</th><th className="p-4">SKU</th><th className="p-4">Price</th><th className="p-4">Stock</th><th className="p-4">Status</th><th className="p-4">Action</th></tr></thead><tbody>{ps.map(p=><tr className="border-t" key={p.id}><td className="p-4"><div className="flex items-center gap-3"><img src={p.image} className="h-12 w-12 rounded-lg object-cover"/><div><b>{p.name}</b><p className="text-xs text-slate-500">{p.slug}</p></div></div></td><td className="p-4">{p.sku}</td><td className="p-4 font-bold">₹{p.price.toLocaleString('en-IN')}</td><td className="p-4">{p.stock}</td><td className="p-4"><span className={`badge ${p.active?'bg-green-100 text-green-700':'bg-slate-100 text-slate-500'}`}>{p.active?'Active':'Inactive'}</span></td><td className="p-4"><div className="flex gap-2"><button className="btn btn-light" onClick={()=>{setForm(p);setEdit(p.id);setOpen(true)}}>Edit</button>{p.active&&<button className="btn btn-danger" onClick={()=>disable(p.id)}>Disable</button>}</div></td></tr>)}</tbody></table></div></div></div>}
