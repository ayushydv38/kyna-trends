import {NextRequest,NextResponse} from 'next/server';import {prisma} from '@/lib/db';export async function POST(req:NextRequest){const id=req.cookies.get('session')?.value;if(id)await prisma.session.delete({where:{id}}).catch(()=>{});const r=NextResponse.json({success:true});r.cookies.set('session','',{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',path:'/',expires:new Date(0)});return r}
