"use client";
import {useEffect,useState} from "react"; import Link from "next/link";
type Stats={products:number;customers:number;orders:number;revenue:number;pending:number;lowStock:number};
export default function AdminDashboard(){const [s,setS]=useState<Stats|null>(null);useEffect(()=>{fetch("/api/admin/dashboard").then(r=>{if(r.status===401||r.status===403)throw Error("unauthorized");return r.json()}).then(setS).catch(()=>{})},[]);if(!s)return <div className="p-8"><h1 className="text-3xl font-black">Dashboard</h1><p className="mt-2 text-slate-500">Loading admin overview...</p></div>;const cards=[['Revenue',`₹${s.revenue.toLocaleString('en-IN')}`],['Orders',s.orders],['Customers',s.customers],['Products',s.products]];return <div className="p-5 md:p-8"><div className="mb-8"><p className="text-sm font-semibold text-blue-600">STORE OVERVIEW</p><h1 className="mt-1 text-3xl font-black">Dashboard</h1><p className="mt-2 text-slate-500">Monitor your store from one place.</p></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([a,b])=><div className="card p-5" key={a}><p className="text-sm text-slate-500">{a}</p><p className="mt-2 text-3xl font-black">{b}</p></div>)}</div><div className="mt-6 grid gap-4 md:grid-cols-2"><div className="card p-6"><h2 className="text-lg font-bold">Needs attention</h2><div className="mt-4 space-y-3"><div className="flex justify-between"><span>Pending / processing orders</span><b>{s.pending}</b></div><div className="flex justify-between"><span>Low-stock products</span><b>{s.lowStock}</b></div></div></div><div className="card p-6"><h2 className="text-lg font-bold">Quick actions</h2><div className="mt-4 flex flex-wrap gap-3"><Link className="btn btn-primary" href="/admin/products">Add / manage products</Link><Link className="btn btn-light" href="/admin/orders">View orders</Link></div></div></div></div>}
