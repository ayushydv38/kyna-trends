"use client";import {useState} from 'react';export default function Register(){const[e,setE]=useState('');async function go(x:React.FormEvent<HTMLFormElement>){x.preventDefault();const f=new FormData(x.currentTarget);const r=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:f.get('name'),email:f.get('email'),password:f.get('password')})});const d=await r.json();if(r.ok)location.href='/';else setE(d.error||'Registration failed')}return <main className="mx-auto max-w-md p-8"><h1 className="text-3xl font-black">Create account</h1>{e&&<p className="mt-3 text-red-600">{e}</p>}<form onSubmit={go} className="mt-6 space-y-3"><input name="name" required minLength={2} placeholder="Full name" className="w-full rounded-xl border p-3"/><input name="email" required type="email" placeholder="Email" className="w-full rounded-xl border p-3"/><input name="password" required minLength={8} type="password" placeholder="Password (8+ characters)" className="w-full rounded-xl border p-3"/><button className="w-full rounded-xl bg-slate-900 p-3 font-bold text-white">Create account</button></form></main>}
